import 'package:flutter/material.dart';

class Sliderpager2 extends StatelessWidget {
  const Sliderpager2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Column(

        ),
      ),

    );
  }
}